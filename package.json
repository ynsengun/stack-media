{
  "name": "cs353",
  "version": "1.0.0",
  "description": "Our World-Changing CS353 Project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "client": "cd client && npm start && cd ..",
    "server": "cd server/bin && nodemon www && cd ../..",
    "setup": "npm i && cd client && npm i && cd .. && cd server && npm i && cd .."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
